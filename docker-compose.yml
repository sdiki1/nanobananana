version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: nanobanana
      POSTGRES_USER: postgress_fucken_strong_user
      POSTGRES_PASSWORD: postgress_fucken_strong_password
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/migrations/001_init.sql:/docker-entrypoint-initdb.d/001_init.sql:ro
      - ./db/migrations/002_action_logs.sql:/docker-entrypoint-initdb.d/002_action_logs.sql:ro
    ports:
      - "5432:5432"

  bot:
    build: .
    env_file: .env
    depends_on:
      - db

  webhook:
    build: .
    command: ["uvicorn", "webhook_app:app", "--host", "0.0.0.0", "--port", "8000"]
    env_file: .env
    depends_on:
      - db
    ports:
      - "8000:8000"

volumes:
  db_data:
